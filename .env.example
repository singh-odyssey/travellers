#############################################
# Environment variables for travellersmeet  #
#############################################

# PostgreSQL database connection string (Prisma)
# Example: postgres://user:password@localhost:5432/travellers
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/travellers"

# NextAuth configuration
# Generate a strong secret: `openssl rand -base64 32`
NEXTAUTH_SECRET="replace-with-strong-secret"
NEXTAUTH_URL="http://localhost:3000"

# (Optional) Additional providers / storage configs can go here
